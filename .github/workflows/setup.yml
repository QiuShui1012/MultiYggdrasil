name: Setup Environment
on:
  workflow_call:
    outputs:
      versions: ${{ fromJson(jobs.setup.outputs.versions).versions }}
      mod_id: ${{ jobs.setup.outputs.mod.id }}
      mod_name: ${{ jobs.setup.outputs.mod.name }}

jobs:
  setup:
    name: Setup Versions
    permissions:
      contents: read
    runs-on: ubuntu-latest
    outputs:
      versions: ${{ steps.versions.outputs.versions }}
      mod_id: ${{ steps.properties.outputs.mod.id }}
      mod_name: ${{ steps.properties.outputs.mod.name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Read Properties
        id: properties
        uses: christian-draeger/read-properties@1.1.1
        with:
          path: gradle.properties
          properties: 'mod.id mod.name'

      - name: Setup Java 25
        uses: actions/setup-java@v5.2.0
        with:
          distribution: zulu
          java-version: 25

      - name: Make Gradle Wrapper Executable
        if: ${{ runner.os != 'Windows' }}
        run: chmod +x ./gradlew

      - name: Build with Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build
        run: ./gradlew :${{ inputs.version }}:build

      - name: Setup Versions
        id: versions
        run: |
          VERSIONS=$(cat ./settings.json | tr -d '\r\n' | tr -d ' ')
          echo "versions=$VERSIONS" >> $GITHUB_OUTPUT
